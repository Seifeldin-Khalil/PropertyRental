const {Router} = require('express');

const AccountController = require ('../Controllers/Account');

const AccountRouter = Router();

//AccountRouter.get('/',AccountController.)

module.exports = AccountRouter;